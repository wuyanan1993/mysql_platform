公共检查项：
	使用到的库、表、列对象是否存在。如果不存在，则报错，errorlevel 为 2
	检查创建对象的名字合法性。如果名字长度大于64个字节，会报错，包括库名、表名、索引名、列名等
insert into sql_review_specificationcontentforsql values
(null, '检查标识符的合法性，如果发现名字中存在数字、字母下划线之外的字符，则会报错。', 1),
(null, '检查 set option 语句类型，目前只支持 set names charsetname 语句，类型为错误，不可配置', 1),
(null, '检查创建表、库对象时使用的字符集是否是参数中所定义的，类型为警告，可配置。', 1),
(null, '检查语句类型，如果是 Create Index 语句，或是 Rename Table 语句，又或是 Drop Index 语句，都建议使用 Alter Table 语句，类型为警告，不可配置', 1),
(null, '检查同一个表是否存在DDL与DML共存的问题，类型为错误，不可配置。', 1),
(null, '检查语句中有没有出现MySQL关键字，类型为警告，可配置', 1),
(null, '检查影响行数是否超过某一个设定的值，类型为警告，可配置', 1);

插入语句检查项
	插入指定的列列表中，同一个列不能出现多次，类型为错误，不可配置
insert into sql_review_specificationcontentforsql values
(null, '必须制定插入列表，也就是要对那几个列指定插入值，如insert into t(id, id2) values (...)，类型为警告，可配置', 2),
(null, '插入列列表与值列表个数相同，二者的个数需要相同，如果没有指定列列表，（因为可配置），则值列表长度妖玉表列数相同，类型为错误，不可配置', 2),
(null, '检查NOT NULL的列，插入的值是不是NULL，类型为错误，不可配置', 2),
(null, '检查查询插入时，查询的列个数，与指定列的列表元素个数是否相同，类型为错误，不可配置', 2),
(null, '检查查询插入语句中的SELECT子句，有没有WHERE条件，类型为警告，可配置', 2),
(null, '检查查询插入语句中的SELECT子句，是不是"SELECT *"，类型为警告，可配置', 2),
(null, '检查查询插入语句中的SELECT子句，有没有LIMIT子句，类型学为警告，可配置', 2),
(null, '检查查询插入语句中的SELECT子句中的ORDER BY子句，排列类是否为RAND()，类型为警告，可配置', 2);

更新、删除语句检查项
insert into sql_review_specificationcontentforsql values
(null, '检查更新删除语句中的SELECT子句有没有WHERE条件，类型为警告，可配置', 3),
(null, '检查更新删除语句中的SELECT子句有没有LIMIT子句，类型为警告，可配置', 3),
(null, '检查更新删除语句中的SELECT自剧中的ORDER BY子句，排列类是否为RAND()，类型为警告，可配置', 3);

表属性检查项
insert into sql_review_specificationcontentforsql values
(null, '检查表的存储引擎是不是InnoDB，有参数可以配置', 4),
(null, '检查表的字符集是不是与参数配置匹配，有参数可以设置支持的字符集', 4),
(null, '检查表是不是有注释，有参数可以设置', 4),
(null, '检查表是不是分区表，有参数可以配置', 4),
(null, '检查LIKE建表时，参照的对象表是不是存在，类型为错误，不可配置。', 4),
(null, '如果建立的是临时表，则必须要以tmp为前缀，类型为错误，不可配置', 4),
(null, '检查创建的列，是否存在重名的列，类型为错误，不可配置', 4),
(null, '检查新表有没有主键，类型为警告，可配置', 4),
(null, '检查自增列初始值是不是1，不是则警告，可配置', 4),
(null, '检查表中是不是存在多个主键，类型为错误，不可配置', 4);

列属性检查项
insert into sql_review_specificationcontentforsql values
(null, '检查BLOB列是不是设置了默认值，如果是则警告，不可配置', 5),
(null, '检查列类型是不是DATETIME，并且其默认值为NOW()，如果是则报错，类型为错误，不可配置', 5),
(null, '检查列设置是不是NOT NULL属性，或者为主键列，但其默认值被设置为NULL，如果是则报错，不可配置', 5),
(null, '检查自增列是不是设置了默认值，如果是则报错，类型为错误，不可配置', 5),
(null, '检查时间类型默认值的合法性，如果与对应的字段类型不匹配，则报错，类型为错误，不可配置。', 5),
(null, '检查列数据类型是不是SET，或者是ENUM，抑或是BIT，类型为警告，可配置', 5),
(null, '检查列有没有注释，类型为警告，可配置', 5),
(null, '检查CHAR类型的字段所定义的长度，如果超过设置，则建议转换为VARCHAR，类型为警告，可配置', 5),
(null, '检查列的类型是不是BLOB，类型为警告，可配置', 5),
(null, '检查列有没有设置NOT NULL属性，类型为警告，可配置', 5),
(null, '检查BLOB类型的列是不是设置了NOT NULL属性，如果设置了则报错，类型为警告，不可配置', 5),
(null, '检查自增列是不是无符号类型，类型为警告，可配置，如果长度小于4，则同事建议转换为INT或者是BIGINT，类型为警告，可配置', 5),
(null, '检查在MySQL5.5及以下版本中，是不是存在多个TIMESTAMP类型的列，类型为错误，不可配置', 5),
(null, '检查DATETIME类型的列，在MySQL5.5及以下版本中，是不是设置了default CURRENT_TIMESTAMP，或者设置了ON UPDATE CURRENT_TIMESTAMP，如果是则报错，类型为错误，不可配置', 5),
(null, '检查TIMESTAMP类型的列，是不是没有设置默认值，但设置了ON UPDATE CURRENT_TIMESTAMP，如果是则报错，类型为错误，不可配置', 5),
(null, '检查不为BLOB类型、不是自增列又不是主键包含的列，是不是设置了默认值，类型为警告，可配置', 5),
(null, '检查不是BLOB类型的列，是不是显示的单独设置了字符集，类型为警告，可配置', 5),
(null, '检查自增列名，是否为id，类型为警告，可配置', 5),
(null, '检查自增列的个数，只能有一个，类型为错误，不可配置', 5);
	
索引属性检查项
insert into sql_review_specificationcontentforsql values
(null, '检查所有是否有名字，如果没有名字则直接报错，属于错误，不属于警告', 6),
(null, '如果索引类型为PRIMARY，则索引名字必须是"PRIMARY"，否则抛出错误', 6),
(null, '检查创建的索引是否为外键，可配置，属于警告', 6),
(null, '检查创建的索引名字是否符合前缀规则，唯一索引需要以"uniq"开头，普通索引需要以"idx_"开头，参数可配置，属于警告', 6),
(null, '检查所有中列的个数，如果超过参数设置就报警告', 6),
(null, '检查主键所包含的列个数，如果超过参数设定数目，则报警告', 6),
(null, '检查主键所包含的列，如果存在劣的类型补位INT，就报警告，有参数可配置', 6),
(null, '检查一个索引所占的空间是不是超过了767哥字节，如果超过则报错，类型为错误，不可配置', 6),
(null, '检查在创建索引的时候，是不是与线上目前已经存在的索引名字存在冲突，如果是则报错，类型为错误，不可配置', 6),
(null, '检查在创建索引或表时，索引个数是不是超过了参数设置值，如果是则报警告', 6),
(null, '检查索引劣种是否包含BLOB列，类型为错误，不可配置', 6),
(null, '检查建表时，是不是存在同名的索引，类型为错误，不可配置', 6),
(null, '检查重复索引，类型为错误，不可配置', 6);

修改表语句检查项
insert into sql_review_specificationcontentforsql values
(null, '检查改表语句中，要修改的表属性类型，目前只支持修改存储引擎，修改注释，修改AUTO INCREMENT、修改字符集，在此之外的都不支持，类型为警告', 7),
(null, '检查在一个语句块中，是不是存在对同一个表的改表语句，类型为警告，可配置', 7),
(null, '检查改表类型，目前支持的有加列、加索引、删列、改表名、修改列、删索引、修改列默认值、修改表属性，以及CONVERT表字符集这几种类型，其他改表类型都不支持。除此之外都报错，类型为警告', 7);